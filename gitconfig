[user]
	email = django.hamster@gmail.com
	name = righ

[color]
        status = auto
        diff = auto
        branch = auto
        interactive = auto
        grep = auto

[core]
        editor = 'vim -c "set fenc=utf-8"'
        excludesfile = ~/.gitignore_global

[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true

[push]
	default = current

[pull]
        rebase = true

[url "https://:x-oauth-basic@github.com/"]
	insteadOf = https://github.com/

